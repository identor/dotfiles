#!/bin/bash

if [[ ! $DB_USER ]]; then
    echo "DB_USER not defined... will use currently logged in user"
    DB_USER=`whoami`
fi

if [[ ! $DB_HOST ]]; then
    echo "DB_HOST not defined... will use localhost"
    DB_HOST=localhost
fi

mysql -A -u$DB_USER -p$DB_PASSWORD -h$DB_HOST $DB_DEFAULT
